# Binaries (anchored to repo root only — do NOT match cmd/api/, etc.)
/bin/
/dist/
/hospital-middleware-system
/api

# Go
*.test
*.out
vendor/

# Env / secrets
.env
.env.*
!.env.example

# IDE
.idea/
.vscode/

# OS
.DS_Store
Thumbs.db

# Kept local-only per project decision — not tracked in git
/docs/
/CLAUDE.md
