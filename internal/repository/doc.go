// Package repository defines the persistence interfaces (HospitalRepository,
// StaffRepository, PatientRepository) and their Postgres implementations, so
// the service layer can be unit-tested against mocks/fakes.
package repository
