// Package config loads and validates application configuration from
// environment variables (DB DSN, JWT secret, HIS base URLs, server port,
// etc). Implemented in Task 04 — see ../../docs/tasks.md.
package config
