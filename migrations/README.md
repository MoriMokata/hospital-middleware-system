# Migrations

SQL migration files for the `hospitals`, `staff`, and `patients` tables (see [`../docs/er-diagram.md`](../docs/er-diagram.md)) are added in **Task 03** — see [`../docs/tasks.md`](../docs/tasks.md).
